[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "HiveMind-twilio-bridge"
description = "Bridge Twilio SMS to a HiveMind node"
readme = "readme.md"
authors = [{ name = "jarbasAI", email = "jarbasai@mailfence.com" }]
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
dynamic = ["version", "dependencies"]

[project.optional-dependencies]
test = ["pytest", "pytest-asyncio", "httpx<1.0"]

[project.urls]
Homepage = "https://github.com/JarbasHiveMind/hivemind-twilio-bridge"

[project.scripts]
hivemind-twilio-bridge = "hivemind_twilio_bridge.__main__:main"

[tool.setuptools]
packages = ["hivemind_twilio_bridge"]
include-package-data = true

[tool.setuptools.dynamic]
version = { attr = "hivemind_twilio_bridge.version.__version__" }
dependencies = { file = ["requirements.txt"] }
